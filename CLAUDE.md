# Instrucciones del proyecto

**Lee [contexto.md](contexto.md) antes de cualquier tarea.** Define el norte del
producto (embudo bottom-up local → global, alertas premium de USD 3, scope LATAM,
interfaz simple) y un protocolo de revisión obligatorio: cada operación debe
verificarse contra su checklist (sección 4) antes de darse por terminada.

Stack: Flask (app factory + blueprints), SQLAlchemy, Flask-Migrate. Correr en local:
ver README.md.
